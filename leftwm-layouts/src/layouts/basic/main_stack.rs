use crate::{
    geometry::{Rect, SplitAxis},
    Util,
};

fn main_stack(
    window_count: usize,
    container: Rect,
    main_count: u8,
    main_percentage: u8,
    main_split: SplitAxis,
    stack_split: SplitAxis,
    reserve_space: bool,
) -> Vec<Rect> {
    todo!()
}
